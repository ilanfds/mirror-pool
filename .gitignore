# Rust
/target
**/target

# Anchor / Solana
.anchor/
test-ledger/
**/*.so

# Keypairs — NEVER commit secret keys
*-keypair.json
**/deploy/*-keypair.json

# Node (TS tests, if any)
node_modules/

# Editor / OS
.idea/
.DS_Store

# Local Claude config
.claude/

# LaTeX build artifacts and the local tectonic binary (keep the .tex source)
docs/whitepaper/tectonic
docs/whitepaper/*.aux
docs/whitepaper/*.log
docs/whitepaper/*.out
docs/whitepaper/*.toc
docs/whitepaper/*.pdf
docs/whitepaper/missfont.log